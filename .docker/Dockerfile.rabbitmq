FROM rabbitmq:3.12-management

ENV RABBITMQ_DEFAULT_USER=user_queue
ENV RABBITMQ_DEFAULT_PASS=pass_queue

ENV RABBITMQ_MEMORY_HIGH_WATERMARK=128MB
ENV RABBITMQ_DISK_FREE_LIMIT=128MB
ENV RABBITMQ_SERVER_ERL_ARGS="+P 25000"

EXPOSE 5672
EXPOSE 15672
